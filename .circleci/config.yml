version: 2.1
parameters:
  test-tag:
    type: string
    default: "billing_info"
orbs:
  browser-tools: circleci/browser-tools@1.3.0
  python: circleci/python@2.0.3

commands:
  install-dependencies:
    steps:
      - python/install-packages:
          pip-dependency-file: requirement.txt
          pkg-manager: pip
  setup-browser:
    steps:
      - browser-tools/install-browser-tools:
          chrome-version: 105.0.5195.102
      - run:
          name: Verify Browser installation
          command: |
            google-chrome --version
            firefox --version
            geckodriver --version
            chromedriver --version
  run-tests:
    steps:
      - run:
          name: "Run Test Cases"
          no_output_timeout: 30m
          command: |
              robot -d Results --output original.xml -v env:prod -i << pipeline.parameters.test-tag >> TGM/ || robot -d Results --prerunmodifier DataDriver.rerunfailed:Results/original.xml --output rerun.xml -v env:prod -i << pipeline.parameters.test-tag >> TGM/
              rebot -d Results --merge Results/original.xml Results/rerun.xml
  prepare-result:
      steps:
        - store_artifacts:
            path: Results
            destination: artifact-file

jobs:
  run-test:
    circleci_ip_ranges: true
    docker:
      - image: cimg/python:3.10.5-browsers
    steps:
      - checkout
      - install-dependencies
      - setup-browser
      - run-tests
      - prepare-result

workflows:
  prepare-and-run-test:
    jobs:
      - run-test