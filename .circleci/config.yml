version: 2.1
parameters:
  test-tag:
    type: string
    default: "all"
orbs:
  browser-tools: circleci/browser-tools@1.3.0
  python: circleci/python@2.0.3

commands:
  install-dependencies:
    steps:
      - python/install-packages:
          pip-dependency-file: requirement.txt
          pkg-manager: pip
  setup-browser:
    steps:
      - browser-tools/install-browser-tools:
          chrome-version: 105.0.5195.102
      - run:
          name: Verify Browser installation
          command: |
            google-chrome --version
            firefox --version
            geckodriver --version
            chromedriver --version
  run-tests:
    steps:
      - run:
          name: "Run Test Cases"
          no_output_timeout: 30m
          command: |
            pabot --processes 2 --testlevelsplit --pabotlib -d Results -v env:prod -i billing_info TGM/

  prepare-result:
      steps:
        - store_artifacts:
            path: Results
            destination: artifact-file

jobs:
  do-setup:
    circleci_ip_ranges: true
    docker:
      - image: cimg/python:3.10.5-browsers
    steps:
      - checkout
      - install-dependencies
      - setup-browser
  run-test-1:
    steps:
      - run-tests
  prepare-final-result:
    steps:
      - prepare-result

workflows:
  prepare-and-run-test:
    jobs:
      - do-setup
      - run-test-1:
          requires:
            - do-setup